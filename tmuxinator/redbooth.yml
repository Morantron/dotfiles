# ~/.tmuxinator/redbooth.yml

name: redbooth
root: ~/hacking/redbooth

pre: sudo lxc-start -n redbooth-dev
windows:
  - bash:
    -
  - frontend:
    - cd teambox-frontend
    - vim
  - backend:
    - cd teambox-hosted
    - vim
  - servers:
    - ssh devenv
